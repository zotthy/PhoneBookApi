CREATE TABLE phoneBook (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    surname VARCHAR(255),
    city VARCHAR(255),
    street VARCHAR(255),
    buildnumber INTEGER,
    aprove BOOLEAN,
    numbertel VARCHAR(11)
);